{
  "name": "example-vite-plugin-env",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:server": "vite build --ssr src/server.ts",
    "build:worker": "npx wrangler deploy --dry-run",
    "dev:worker": "npx wrangler dev"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-wite": "workspace:*",
    "typescript": "^5.7.0"
  }
}
